<%= form_for(@positive) do |f| %>
  <% if @positive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@positive.errors.count, "error") %> prohibited this positive from being saved:</h2>

      <ul>
      <% @positive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student %><br \>
    <%= f.select "student_id", options_from_collection_for_select(@users, "id", "username") %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
