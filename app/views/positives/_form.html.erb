<%= form_for(@positive) do |f| %>
  <% if @positive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@positive.errors.count, "error") %> prohibited this positive from being saved:</h2>

      <ul>
      <% @positive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.select :type, ["Plus", "Plusplus", "Minus"] %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
    <%= f.hidden_field :rubric_line_item_id, :value => @rubric_line_item_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
